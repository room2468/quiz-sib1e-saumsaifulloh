#!/bin/env bash

# HINT: You can test the result of this script using the following command:
# printf "install.sh\nbackup.sh\nsystem.log\nnginx.conf\nreadme.txt\n" | ./file-classifier

# This script reads a list of filenames (one per line) and counts them by type.

scripts=0
logs=0
configs=0
others=0

while IFS= read -r filename; do
    # Step 1: Check the file extension using a case statement.
    #   Use: case "$filename" in
    #
    #   Match patterns with wildcard *:
    #     *.sh)   matches any filename ending in .sh
    #     *.log)  matches any filename ending in .log
    #     *.conf) matches any filename ending in .conf
    #     *)      matches everything else (the "others" category)
    #
    #   Example:
    #     case "$filename" in
    #         *.sh)
    #             (( scripts++ ))
    #             ;;
    #         *.log)
    #             (( logs++ ))
    #             ;;
    #         *.conf)
    #             (( configs++ ))
    #             ;;
    #         *)
    #             (( others++ ))
    #             ;;
    #     esac


    # TODO: Replace this line with your case statement
    case "$filename" in
        *.sh)
            (( scripts++ ))
            ;;
        *.log)
            (( logs++ ))
            ;;
        *.conf)
            (( configs++ ))
            ;;
        *)
            (( others++ ))
            ;;
    esac
done

echo "Scripts (.sh): $scripts"
echo "Logs (.log): $logs"
echo "Configs (.conf): $configs"
echo "Others: $others"
